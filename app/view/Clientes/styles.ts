import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textoSemClientes: {
    color: "#000",
    fontSize: 18,
    textAlign: "center",
    marginTop: 100,
    fontWeight: "bold"
  }
});

export default styles;